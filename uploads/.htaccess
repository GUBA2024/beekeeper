# Prevent execution of PHP (or any script) inside the uploads folder.
# Uploaded files are served as static content only.
<FilesMatch "\.php\d*$">
    Require all denied
</FilesMatch>

Options -ExecCGI
AddType text/plain .php .php3 .php4 .php5 .php7 .php8 .phtml .phar
